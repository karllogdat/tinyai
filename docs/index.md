**program:**

![program](diagram/program.png)

```
program  ::= stmt-list
```

**stmt-list:**

![stmt-list](diagram/stmt-list.png)

```
stmt-list
         ::= stmt* e
```

referenced by:

* program
* stmt-block

**stmt:**

![stmt](diagram/stmt.png)

```
stmt     ::= ( decl | expr | assign | 'print' '(' expr ')' )? ';'
           | if-stmt
           | while-stmt
           | for-stmt
           | stmt-block
```

referenced by:

* for-stmt
* if-stmt
* stmt-list
* while-stmt

**stmt-block:**

![stmt-block](diagram/stmt-block.png)

```
stmt-block
         ::= '{' stmt-list? '}'
```

referenced by:

* stmt

**decl:**

![decl](diagram/decl.png)

```
decl     ::= type ident ( '=' expr )?
```

referenced by:

* for-init
* stmt

**type:**

![type](diagram/type.png)

```
type     ::= 'int'
           | 'float'
           | 'bool'
           | 'char'
           | 'string'
```

referenced by:

* decl

**assign:**

![assign](diagram/assign.png)

```
assign   ::= ident '=' ( expr | 'input' '(' STRING_LIT_TOKEN ')' )
```

referenced by:

* for-init
* for-iter
* stmt

**if-stmt:**

![if-stmt](diagram/if-stmt.png)

```
if-stmt  ::= 'if' '(' expr ')' stmt ( 'elif' '(' expr ')' stmt )* ( 'else' stmt )?
```

referenced by:

* stmt

**while-stmt:**

![while-stmt](diagram/while-stmt.png)

```
while-stmt
         ::= 'while' '(' expr ')' stmt
```

referenced by:

* stmt

**for-stmt:**

![for-stmt](diagram/for-stmt.png)

```
for-stmt ::= 'for' '(' for-init ';' expr ';' for-iter ')' stmt
```

referenced by:

* stmt

**for-init:**

![for-init](diagram/for-init.png)

```
for-init ::= decl
           | assign
           | e
```

referenced by:

* for-stmt

**for-iter:**

![for-iter](diagram/for-iter.png)

```
for-iter ::= assign
           | expr
           | e
```

referenced by:

* for-stmt

**expr:**

![expr](diagram/expr.png)

```
expr     ::= land-expr ( 'or' land-expr )*
```

referenced by:

* assign
* decl
* for-iter
* for-stmt
* func-tail
* if-stmt
* primary-expr
* stmt
* while-stmt

**land-expr:**

![land-expr](diagram/land-expr.png)

```
land-expr
         ::= eq-expr ( 'and' eq-expr )*
```

referenced by:

* expr

**eq-expr:**

![eq-expr](diagram/eq-expr.png)

```
eq-expr  ::= rel-expr ( ( '==' | '!=' ) rel-expr )*
```

referenced by:

* land-expr

**rel-expr:**

![rel-expr](diagram/rel-expr.png)

```
rel-expr ::= add-expr ( ( '>' | '<' | '>=' | '<=' ) add-expr )*
```

referenced by:

* eq-expr

**add-expr:**

![add-expr](diagram/add-expr.png)

```
add-expr ::= mult-expr ( ( '+' | '-' ) mult-expr )*
```

referenced by:

* rel-expr

**mult-expr:**

![mult-expr](diagram/mult-expr.png)

```
mult-expr
         ::= mult-expr ( '*' | '/' | '%' | '//' ) unary-expr ( '**' unary-expr )*
```

referenced by:

* add-expr
* mult-expr

**unary-expr:**

![unary-expr](diagram/unary-expr.png)

```
unary-expr
         ::= ( 'not' | '-' )* primary-expr
```

referenced by:

* mult-expr

**primary-expr:**

![primary-expr](diagram/primary-expr.png)

```
primary-expr
         ::= ident func-tail?
           | literal
           | '(' expr ')'
```

referenced by:

* unary-expr

**func-tail:**

![func-tail](diagram/func-tail.png)

```
func-tail
         ::= '(' ( expr ( ',' expr )* )? ')'
```

referenced by:

* primary-expr

**literal:**

![literal](diagram/literal.png)

```
literal  ::= INT_LIT_TOKEN
           | FLOAT_LIT_TOKEN
           | BOOL_LIT_TOKEN
           | CHAR_LIT_TOKEN
           | STRING_LIT_TOKEN
```

referenced by:

* primary-expr

**ident:**

![ident](diagram/ident.png)

```
ident    ::= IDENT_TOKEN
```

referenced by:

* assign
* decl
* primary-expr

## 
![rr-2.6](diagram/rr-2.6.png) <sup>generated by [RR - Railroad Diagram Generator][RR]</sup>

[RR]: https://www.bottlecaps.de/rr/ui